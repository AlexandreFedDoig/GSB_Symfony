<form method="post" action="{{ path('interagir_delete', {'medPerturbateur': interagir.medPerturbateur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interagir.medPerturbateur) }}">
    <button class="btn">Delete</button>
</form>
