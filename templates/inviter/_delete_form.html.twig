<form method="post" action="{{ path('inviter_delete', {'acNum': inviter.acNum}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inviter.acNum) }}">
    <button class="btn">Delete</button>
</form>
