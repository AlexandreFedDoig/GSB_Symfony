{% extends 'base.html.twig' %}

{% block title %}Labo{% endblock %}

{% block body %}
    <h1>Labo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>LabCode</th>
                <td>{{ labo.labCode }}</td>
            </tr>
            <tr>
                <th>LabNom</th>
                <td>{{ labo.labNom }}</td>
            </tr>
            <tr>
                <th>LabChefvente</th>
                <td>{{ labo.labChefvente }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('labo_index') }}">back to list</a>

    <a href="{{ path('labo_edit', {'labCode': labo.labCode}) }}">edit</a>

    {{ include('labo/_delete_form.html.twig') }}
{% endblock %}
