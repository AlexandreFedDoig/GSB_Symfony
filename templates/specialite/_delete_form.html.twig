<form method="post" action="{{ path('specialite_delete', {'speCode': specialite.speCode}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialite.speCode) }}">
    <button class="btn">Delete</button>
</form>
