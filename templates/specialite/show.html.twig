{% extends 'base.html.twig' %}

{% block title %}Specialite{% endblock %}

{% block body %}
    <h1>Specialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>SpeCode</th>
                <td>{{ specialite.speCode }}</td>
            </tr>
            <tr>
                <th>SpeLibelle</th>
                <td>{{ specialite.speLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specialite_index') }}">back to list</a>

    <a href="{{ path('specialite_edit', {'speCode': specialite.speCode}) }}">edit</a>

    {{ include('specialite/_delete_form.html.twig') }}
{% endblock %}
