<form method="post" action="{{ path('medicament_delete', {'medDepotlegal': medicament.medDepotlegal}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicament.medDepotlegal) }}">
    <button class="btn">Delete</button>
</form>
