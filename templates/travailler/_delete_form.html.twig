<form method="post" action="{{ path('travailler_delete', {'visMatricule': travailler.visMatricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ travailler.visMatricule) }}">
    <button class="btn">Delete</button>
</form>
