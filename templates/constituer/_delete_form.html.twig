<form method="post" action="{{ path('constituer_delete', {'medDepotlegal': constituer.medDepotlegal}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ constituer.medDepotlegal) }}">
    <button class="btn">Delete</button>
</form>
