{% extends 'base.html.twig' %}

{% block title %}Offrir{% endblock %}

{% block body %}
    <h1>Offrir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>VisMatricule</th>
                <td>{{ offrir.visMatricule }}</td>
            </tr>
            <tr>
                <th>RapNum</th>
                <td>{{ offrir.rapNum }}</td>
            </tr>
            <tr>
                <th>MedDepotlegal</th>
                <td>{{ offrir.medDepotlegal }}</td>
            </tr>
            <tr>
                <th>OffQte</th>
                <td>{{ offrir.offQte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offrir_index') }}">back to list</a>

    <a href="{{ path('offrir_edit', {'visMatricule': offrir.visMatricule}) }}">edit</a>

    {{ include('offrir/_delete_form.html.twig') }}
{% endblock %}
