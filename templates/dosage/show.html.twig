{% extends 'base.html.twig' %}

{% block title %}Dosage{% endblock %}

{% block body %}
    <h1>Dosage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>DosCode</th>
                <td>{{ dosage.dosCode }}</td>
            </tr>
            <tr>
                <th>DosQuantite</th>
                <td>{{ dosage.dosQuantite }}</td>
            </tr>
            <tr>
                <th>DosUnite</th>
                <td>{{ dosage.dosUnite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dosage_index') }}">back to list</a>

    <a href="{{ path('dosage_edit', {'dosCode': dosage.dosCode}) }}">edit</a>

    {{ include('dosage/_delete_form.html.twig') }}
{% endblock %}
