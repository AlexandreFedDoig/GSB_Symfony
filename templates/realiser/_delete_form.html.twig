<form method="post" action="{{ path('realiser_delete', {'acNum': realiser.acNum}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realiser.acNum) }}">
    <button class="btn">Delete</button>
</form>
