{% extends 'base.html.twig' %}

{% block title %}Secteur{% endblock %}

{% block body %}
    <h1>Secteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>SecCode</th>
                <td>{{ secteur.secCode }}</td>
            </tr>
            <tr>
                <th>SecLibelle</th>
                <td>{{ secteur.secLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('secteur_index') }}">back to list</a>

    <a href="{{ path('secteur_edit', {'secCode': secteur.secCode}) }}">edit</a>

    {{ include('secteur/_delete_form.html.twig') }}
{% endblock %}
