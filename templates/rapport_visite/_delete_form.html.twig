<form method="post" action="{{ path('rapport_visite_delete', {'visMatricule': rapport_visite.visMatricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_visite.visMatricule) }}">
    <button class="btn">Delete</button>
</form>
