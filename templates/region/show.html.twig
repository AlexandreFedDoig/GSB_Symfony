{% extends 'base.html.twig' %}

{% block title %}Region{% endblock %}

{% block body %}
    <h1>Region</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RegCode</th>
                <td>{{ region.regCode }}</td>
            </tr>
            <tr>
                <th>SecCode</th>
                <td>{{ region.secCode }}</td>
            </tr>
            <tr>
                <th>RegNom</th>
                <td>{{ region.regNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('region_index') }}">back to list</a>

    <a href="{{ path('region_edit', {'regCode': region.regCode}) }}">edit</a>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
