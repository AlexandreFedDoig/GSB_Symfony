<form method="post" action="{{ path('type_individu_delete', {'tinCode': type_individu.tinCode}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_individu.tinCode) }}">
    <button class="btn">Delete</button>
</form>
